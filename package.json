{
  "name": "cis-audit-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "backend": "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
